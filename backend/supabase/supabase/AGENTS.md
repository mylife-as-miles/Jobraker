# Agent Instructions

Do **not** edit or deploy files under this directory for production Jobraker work.

The canonical Supabase implementation is:

- `backend/supabase/functions/`
- `backend/supabase/migrations/`
- `backend/supabase/config.toml`

Treat this nested directory as a legacy snapshot only. Move any still-required behavior into the canonical tree instead of making parallel changes here.
